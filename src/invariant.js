// @flow
// @format
// The invariant package I'm bringing in doesn't... work. This is a hook to make it work properly for me.
function invariant(cond /*:bool*/, msg /*:string*/) {
  // TODO: SOMETHING.
}

module.exports = invariant;
